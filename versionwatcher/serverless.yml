service: versionwatcher

provider:
    name: aws
    runtime: nodejs4.3
    profile: versionwatcher
    stage: stage
    region: eu-west-1

functions:
    trackWP:
        handler: wpHandler.trackWP
        description: Track PHP dependencies
        events:
            - http:
                path: watcher/track-wp
                method: post
