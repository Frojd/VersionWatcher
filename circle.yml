machine:
    node:
        version: 4.3

test:
    override:
        - npm run test

deployment:
    stage:
        branch: develop
        commands:
            - cd versionwatcher && ../node_modules/serverless/bin/serververless deploy
