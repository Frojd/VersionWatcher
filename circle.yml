machine:
    node:
        version: 6.10

test:
    override:
        - npm run test

deployment:
    stage:
        branch: develop
        commands:
            - cd versionwatcher && ../node_modules/serverless/bin/serverless deploy
